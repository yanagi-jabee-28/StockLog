# StockLog Architecture Guide (FSD: Feature-Sliced Design)

本プロジェクトは、スケーラビリティと長期的な保守性を確保するため、**Feature-Sliced Design (FSD)** の思想に基づき、徹底的な責務分離（Separation of Concerns）を行っています。

UIコンポーネント、ビジネスロジック、データ永続化層の「癒着」を防ぎ、「どこに何があるか」「どこに新機能を追加すべきか」が直感的に判断できる体制を構築しています。

---

## 1. 階層構造の定義 (Layers)

FSDでは、**下位レイヤーは上位レイヤーを参照してはいけません（単方向依存）**。これにより、予期せぬ副作用や循環参照を防ぎます。

| レイヤー (Layer) | 役割 | 依存許可範囲 |
| :--- | :--- | :--- |
| **1. `app`** | アプリ全体の基盤。エントリーポイント、Provider設定、グローバルスタイル、ルーティングなど。 | すべてのレイヤーを参照可 |
| **2. `features`** | ユーザーが実行する具体的な「アクション」や「ユースケース」。(例: AddItem, LogMeal) | `entities`, `shared` を参照可 |
| **3. `entities`** | ビジネスドメインの核となる概念（Inventory, Meal）。データ構造、操作モデル、ドメイン固有のUI部品。 | `shared` のみ参照可 |
| **4. `shared`** | プロジェクト全体で再利用されるドメイン非依存の汎用コード。（汎用ボタン、ストレージユーティリティなど） | 依存なし (他のレイヤーを参照不可) |

---

## 2. セグメントの役割 (Segments)

各レイヤー内の機能群（スライス）は、さらに **Segment** という標準化されたディレクトリに分割されます。
これにより、特定の機能の中での「UI」と「ロジック」の役割が明確に分離されます。

| セグメント名 | 役割と格納するもの |
| :--- | :--- |
| **`ui/`** | 視覚的な表現のみを担当する React コンポーネント。状態（State）を極力持たない "Dumb" コンポーネントが理想。 |
| **`model/`** | その機能やドメインに関するビジネスロジック、グローバルな状態管理、データの同期・更新処理。 |
| **`lib/`** | 内部で完結する純粋関数（ピュアファンクション）、データの整形処理、またはUIに関連しないカスタムフック。 |
| **`api/`** | 外部システム（APIサーバー、Local Storage、Indexed DBなど）との通信やデータ取得・保存の抽象化（Repository等）。 |

---

## 3. StockLogの全体ディレクトリマップ

現在のプロジェクトの全体像と役割の対応表です。

```text
src/
├── app/                  # 【第1層】アプリケーション全体の基盤
│   ├── providers/        # Context Providers (DataProvider, UIProvider 等)
│   └── styles/           # グローバルスタイル (index.css)
│
├── features/             # 【第2層】ユーザーアクション・ユースケース
│   ├── add-item/         # 「在庫を追加する」機能 (フォーム、バリデーション)
│   │   ├── lib/          #   └ useAddItemForm.ts (フォームの状態管理)
│   │   └── ui/           #   └ AddItemModal.tsx (追加用UI)
│   ├── log-meal/         # 「献立を記録する」機能
│   ├── ai-selection/     # 「AI用にテキスト生成する」機能
│   ├── inventory/        # 「在庫一覧を操作・フィルタリングする」機能
│   └── settings/         # 「設定を変更・データ移行する」機能
│
├── entities/             # 【第3層】ビジネスドメイン (コアデータ)
│   ├── inventory/        # [ドメイン] 在庫アイテム
│   │   ├── api/          #   └ Repository実装 (localStorageへのCRUD)
│   │   ├── lib/          #   └ useInventory.ts (在庫データ取得用フック)
│   │   ├── model/        #   └ 在庫に関するビジネスルール・型定義
│   │   └── ui/           #   └ InventoryItemCard.tsx (在庫1件の表示部品)
│   └── meal/             # [ドメイン] 食事・献立
│       └── ...
│
├── shared/               # 【第4層】ドメインに依存しない汎用インフラ
│   ├── lib/              # 汎用ロジック
│   │   ├── hooks/        #   └ useModalNavigation (汎用モーダル管理)
│   │   ├── storage.ts    #   └ LocalStorageのラッパー
│   │   └── logger.ts     #   └ アプリ全体のロギング機構
│   ├── ui/               # 汎用コンポーネント
│   │   ├── layout/       #   └ Sidebar, Header 等の基本レイアウト
│   │   └── SuggestionList.tsx # 汎用サジェストUI
│   └── types.ts          # アプリ全体で共有する基本型 (ID型など)
│
├── constants.ts          # プロジェクト全体の定数 (APP_VERSION, カテゴリ定義等)
├── main.tsx              # React エントリーポイント
└── App.tsx               # ルートコンポーネント
```

---

## 4. 開発時の意思決定ツリー (どこに何を配置すべきか？)

新機能や新しいコンポーネントを作る際、以下のフローで配置場所を決定します。

1. **これはプロジェクト全体の設定や基盤か？**
   ▶ `app/` へ。
2. **これは、ドメイン知識（在庫、食事など）を全く含まない汎用的な部品（ボタン、テキストボックス、汎用フック）か？**
   ▶ `shared/` へ。
3. **これは「在庫」や「食事」そのものを表すデータ構造や、それを表示するための基本的な部品（ItemCardなど）か？**
   ▶ `entities/` (該当するドメイン内) へ。
4. **これはユーザーが特定の目的を達成するための「一連の操作・画面」や「複合的な機能（追加フォーム、フィルタリングなど）」か？**
   ▶ `features/` へ。

### 💡 features と entities の境界に迷った時
- **entities** には「名詞（モノ）」を置きます。（例: 「在庫のデータ」「在庫カード」）
- **features** には「動詞（コト）」を置きます。（例: 「在庫を追加する」「在庫を検索する」）
- 在庫一覧画面を作る場合、在庫データ自体は `entities/inventory/lib` から取得し、画面の検索バーやフィルタリング状態の管理は `features/inventory/` 側で行う、という分業になります。

---

## 5. ファイル・ディレクトリ命名に関する厳格な方針

### 1. 同名フォルダの積極的な利用
各スライス（フォルダ）の直下には、必ず `ui`, `model`, `lib`, `api` といった標準セグメント名のフォルダを作成します。
- ✅ `src/entities/inventory/ui/`
- ✅ `src/features/add-item/ui/`

### 2. ファイル名の明確化（ドメインプレフィックス）
以前の平坦なアーキテクチャの名残を防ぐため、ロジック系のファイル名は「何のための部品か」が一目でわかるように命名します。

| NG (用途が不明確) | OK (明確な命名) | 配置場所 |
| :--- | :--- | :--- |
| `useForm.ts` | `useAddItemForm.ts` | `features/add-item/lib/` |
| `useData.ts` | `useInventory.ts` | `entities/inventory/lib/` |

### 3. 物理的限界（認知的負荷の制御）
- **1ファイル約200行の目安**: 1つのファイルが200行を超える場合、それは「複数の責務を持っている（神クラス化している）」兆候です。
- UIの巨大化に気づいた場合、直ちに「純粋な描画部分」を別のサブコンポーネントとして `ui/` 内で分割するか、ロジックを `lib/` のカスタムフックに逃がすリファクタリングを実施してください。

---

## 6. このドキュメントの運用ルール

この `ARCHITECTURE.md` は、すべての開発者・AIエージェントが従うべき**「法」**です。

1. **ルールの検証**: 構造を変更する際は、FSDの「単方向依存（shared は entities を参照できない等）」を破壊していないか必ず検証すること。
2. **自己進化**: 新たな技術要件によりルールを拡張する必要がある場合は、まずこのドキュメントを更新し、全体の合意（定義）を形成してからコードを変更すること。
3. **波及確認**: 本書の変更は、AIプロンプト（`GEMINI.md`, `AGENTS.md`）と矛盾が生じないようにメンテナンスすること。
